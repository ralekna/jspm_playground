/**
 * Created by rytis on 2015-04-22.
 */

describe('String utils specs 2', () => {
  it('just have to run 2', () => {
    expect(true).toBe(true);
  });
});

describe('Various es6 templates functionality', () => {
  it('should interpolate local vars', () => {
    debugger;
    let one = '1';
    let two = '2';
    // expect(`${one}${two}`).toBe('12');
  });
});
